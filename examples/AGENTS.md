# AGENTS.md

Project: replace-me

Architecture:
- describe the important directories and boundaries here

Rules:
- never edit generated files unless the ticket explicitly requires it
- preserve backwards compatibility unless the ticket explicitly changes it
- do not commit; PMC owns Git state

Canonical verification:
- use the commands in poorman.yaml
